// TODO(seed): insert a default verifier_set (threshold 2) and three demo verifier
// pubkeys (Phantom keypairs are fine for devnet). Run with `tsx scripts/seed-verifiers.ts`.
// Requires NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY in env.

export {};

async function main(): Promise<void> {
  throw new Error('Not implemented');
}

void main();
