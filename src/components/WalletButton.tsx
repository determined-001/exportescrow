'use client';

import { WalletMultiButton } from '@solana/wallet-adapter-react-ui';

export function WalletButton() {
  // TODO(wallet): style to match the rest of the app once we settle on a theme.
  return <WalletMultiButton />;
}
